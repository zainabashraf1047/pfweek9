using namespace std;
main()
{
   // bool isfound=false;
    int size;
    cout<<"Enter size of array: ";
    cin>>size;
    int numbers[size];
    int another;
    int multiply;
    
    for(int idx=0;idx<size;idx++)
    {
        cout<<"Enter number: ";
        cin>>numbers[idx];
        
    }
    cout<<"Enter another no: ";
    cin>>another;
}